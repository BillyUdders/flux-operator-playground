apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: bill-2 
  region: us-west-2
  version: "1.33"

managedNodeGroups:
  - name: ng-1
    instanceType: m5.large
    desiredCapacity: 2
    minSize: 2
    maxSize: 4
